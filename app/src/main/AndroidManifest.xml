<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.edu.pu.csie.s1040345.smart_gophers">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GamelistActivity"></activity>
        <activity android:name=".menuActivity"></activity>
        <activity android:name=".ColorActivity"></activity>
        <activity android:name=".FruitActivity"></activity>
        <activity android:name=".FruitgameActivity"></activity>
        <activity android:name=".ColorgameActivity"></activity>
        <activity android:name=".logoutActivity"></activity>

    </application>

</manifest>